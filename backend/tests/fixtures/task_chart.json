{
  "id": "",
  "title": "Top spenders via SQL then summarise",
  "description": "Subagent step computes a SQL artifact; downstream code step references its artifact_id.",
  "source_session_id": null,
  "variables": [
    {
      "name": "database_name",
      "type": "string",
      "label": "Database Name",
      "default": "Chinook",
      "required": true
    }
  ],
  "steps": [
    {
      "id": "s1",
      "kind": "subagent",
      "title": "Calculate Top 10 Spenders via SQL",
      "subagent": "sql-agent",
      "prompt": "Query the {{var.database_name}} DB for the top 10 customers by total spend. Return the result as a table artifact.",
      "output_name": "spenders_text",
      "output_kind": "text"
    },
    {
      "id": "s2",
      "kind": "code",
      "title": "Echo artifact id from prior step",
      "code": "out({'artifact_id': '{{s1.artifact_id}}'})",
      "output_name": "echo",
      "output_kind": "json"
    }
  ],
  "created_at": null,
  "updated_at": null
}
